import { ComponentFixture, TestBed } from '@angular/core/testing';

import { EditorContaiterComponent } from './editor-contaiter.component';

describe('EditorContaiterComponent', () => {
  // let component: EditorContaiterComponent;
  // let fixture: ComponentFixture<EditorContaiterComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ EditorContaiterComponent ]
  //   })
  //   .compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(EditorContaiterComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
